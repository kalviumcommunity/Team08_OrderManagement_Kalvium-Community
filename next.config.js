/**
 * Next.js Application Configuration
 * Enables React Strict Mode for detecting side effects and deprecated patterns.
 * 
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
