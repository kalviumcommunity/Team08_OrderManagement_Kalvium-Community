datasource db {
<<<<<<< HEAD
  provider = "sqlite"
=======
  provider = "postgresql"
>>>>>>> c14499a (feat(auth): add backend prisma schema, seed data, and NextAuth credentials routes)
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

<<<<<<< HEAD
=======
enum Role {
  CUSTOMER
  OWNER
  MANAGER
  AUDITOR
}

enum OrderStatus {
  PENDING
  PREPARING
  READY
}

>>>>>>> c14499a (feat(auth): add backend prisma schema, seed data, and NextAuth credentials routes)
model User {
  id           String         @id @default(uuid())
  name         String
  email        String         @unique
  passwordHash String
<<<<<<< HEAD
  role         String         @default("CUSTOMER") // Role types: "CUSTOMER", "OWNER", "MANAGER", "AUDITOR"
=======
  role         Role           @default(CUSTOMER)
>>>>>>> c14499a (feat(auth): add backend prisma schema, seed data, and NextAuth credentials routes)
  createdAt    DateTime       @default(now())
  orders       Order[]
  inventoryLogs InventoryLog[]
}

model Product {
  id           String         @id @default(uuid())
  name         String
  stock        Int            @default(0)
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt
  orderItems   OrderItem[]
  inventoryLogs InventoryLog[]
}

model Order {
  id           String         @id @default(uuid())
  userId       String
  user         User           @relation(fields: [userId], references: [id])
  customerName String         // Cache customer name for quick read
<<<<<<< HEAD
  status       String         @default("PENDING") // Status types: "PENDING", "PREPARING", "READY"
=======
  status       OrderStatus    @default(PENDING)
>>>>>>> c14499a (feat(auth): add backend prisma schema, seed data, and NextAuth credentials routes)
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt
  items        OrderItem[]
}

model OrderItem {
  id        String  @id @default(uuid())
  orderId   String
  order     Order   @relation(fields: [orderId], references: [id], onDelete: Cascade)
  productId String
  product   Product @relation(fields: [productId], references: [id])
  quantity  Int
}

model InventoryLog {
  id               String   @id @default(uuid())
  productId        String
  product          Product  @relation(fields: [productId], references: [id])
  ownerId          String?
  owner            User?    @relation(fields: [ownerId], references: [id])
  previousQuantity Int
  newQuantity      Int
  changeAmount     Int
  reason           String
  timestamp        DateTime @default(now())
}
